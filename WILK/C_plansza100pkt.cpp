#include <iostream>

using namespace std;

int n = 0, m = 0;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> n >> m;

    cout << n / 2 + m / 2 << '\n';

    return 0;
}