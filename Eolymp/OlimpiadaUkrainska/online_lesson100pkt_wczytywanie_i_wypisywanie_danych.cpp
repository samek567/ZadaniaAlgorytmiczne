#include <iostream>

using namespace std;

int s = 0, a = 0;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    cin >> s >> a;

    cout << s-a << '\n';

    return 0;
}